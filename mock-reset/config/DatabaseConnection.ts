import DatabaseConnection from '../types/DatabaseConfig.js';

const DatabaseConnectionObject: DatabaseConnection = {
    HOST: 'localhost',
    USERNAME: 'root',
    PASSWORD: '',
    PORT: 3306
};

export default DatabaseConnectionObject;