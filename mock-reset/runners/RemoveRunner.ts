import Runner from '../models/Runner';

class RemoveRunner extends Runner {
  
    public executeCommand() : void {
        console.log('Creating a RemoveRunner');
    }
}

export default RemoveRunner;